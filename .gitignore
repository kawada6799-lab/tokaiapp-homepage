# build.mjs が作るプレビュー（parts/ と pages/ から毎回組み立て直せる）
preview/

# pack-theme.mjs が作る組み立て結果
build/

# OS が勝手に作るもの
.DS_Store
Thumbs.db

# 写真素材（写真AC等は再配布不可なので、公開リポジトリには入れない。
# テーマzipには pack-theme.mjs が同梱する）
assets/img/photos/
