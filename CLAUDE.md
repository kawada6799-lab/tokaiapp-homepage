# 東海App コーポレートサイト — 編集の約束ごと

tokaiapp.com。WordPress で運用する、9ページ構成のコーポレートサイト。

## 何を編集するか

| やりたいこと | 触るファイル |
|---|---|
| ページの文章・ブロックの追加や並べ替え | `pages/<ページ>.html` |
| ヘッダー・フッター・CTA帯 | `parts/*.html` |
| 色・余白・文字サイズを変える | `assets/css/tokens.css` |
| レイアウトや部品の見た目を変える | `assets/css/style.css` |
| 動きを変える | `assets/js/main.js` |
| ページを増やす | `pages/` にHTMLを足し、`pages.json` に1行足す |
| WordPress側の挙動 | `wordpress/tokaiapp/` |

**`wordpress/tokaiapp/` にページのHTMLを書かないこと。**
テーマは `pages/` と `parts/` を読むだけの殻にしてある。
二重管理が始まると必ずずれる。

## 守ること

1. **事業内容・実績・料金・連絡先を創作しない。**
   これは実在する会社の公開サイト。確認できていない情報は
   `★要確認` コメントを添えたプレースホルダのままにして、ユーザーに確認を取る。
   数値は `◯` で伏せてある。埋める前に必ず出典を確認する

2. **オレンジ（`--orange`）は問い合わせ導線だけに使う。**
   「オレンジ＝問い合わせ」と閲覧者に覚えてもらうための配色。
   見出しや装飾に流用しない

3. **CSSに色や余白の生の値を直接書かない。** 必ず `tokens.css` の変数を使う。
   例外は、サービス詳細ページの `style="--svc: #xxxxxx"`（識別色の指定）だけ

4. **ブランドカラーは現行ロゴ「TA」由来のネイビーとブルー。**
   ドウイナビ（製品）のティール `#0f766e` と混同しない。
   ティールを使ってよいのは、患者画面の再現モックの中だけ

5. **サービス詳細3ページは同じ骨格を保つ。**
   変わるのは識別色と中身だけ。片方だけ構成を変えない

6. **ページ間のリンクは本番URL**（`/price/` など）で書く。
   `build.mjs` がプレビュー用に変換する

7. **画像の参照は `{{ASSETS}}/img/xxx.png` と書く。** 相対パスで書かないこと。
   `/service/douinavi/` のような下層ページでずれる。
   `build.mjs` と `functions.php` の両方が置き換える

8. **実写素材をこのリポジトリに置かない。** Public なので履歴から消せない。
   写真は WordPress のメディアにアップロードし、絶対URLで参照する
   （`docs/画像の入れ方.md`）

9. **`preview/` と `build/` は触らない。** どちらも生成物

10. **国外アクセス制限（`wordpress/tokaiapp/inc/geo-guard.php`）を
   フェイルクローズに変えないこと。** 国コードのヘッダが届かない環境では
   何もしない設計です。「判定できないから止める」にすると、
   設定変更のたびにユーザー自身が締め出されます。
   仕様と弱点は `docs/セキュリティ_国外アクセス制限.md` にあります

## ユーザーが自分で文言を直したと言ったとき

WordPressの「テーマファイルエディター」で直した内容は、
**次に zip を上書きすると消えます。** その旨を伝えたうえで、
同じ変更を `pages/` ・ `parts/` にも反映してコミットすること。
手順は `docs/文言の直し方.md`。

## 確認のしかた

```bash
node build.mjs        # preview/ に10ページ書き出す
```

`preview/index.html` をブラウザで開く。見た目を変えたら、次の幅で崩れていないか見る:
1440px（PC）／ 768px（タブレット）／ 390px（スマホ）

## 本番反映

```bash
node pack-theme.mjs   # build/tokaiapp.zip
```

WordPress の 外観 → テーマ → 新規追加 → テーマのアップロード。
**反映前に必ずバックアップを取る**（`docs/WordPress反映手順.md`）。

## 出典

このサイトの設計は、Artifact「東海App リニューアル設計」に基づく。
配色・ブロック順序・その意図はそこで決まっている。
ドウイナビの仕様は `kawada6799-lab/moview` リポジトリの `docs/` が一次情報。
